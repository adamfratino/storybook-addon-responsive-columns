export const ADDON_ID = "storybook/responsive-columns";
export const PANEL_ID = `${ADDON_ID}/panel`;
export const PARAM_KEY = `columns`;

export const EVENTS = {
  RESULT: `${ADDON_ID}/result`,
  REQUEST: `${ADDON_ID}/request`,
  CLEAR: `${ADDON_ID}/clear`,
};
